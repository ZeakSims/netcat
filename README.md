# netcat

A single Python code whith a listener and a talker. Listener When  invoked  with  a  '-L'   flag  the  program  listens  on  the  user  supplied  host,port, and protocol and a talker When invoked with a '-S'  flag the program echoes stdin to the user supplied host,port, and protocol. 
